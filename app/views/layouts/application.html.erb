<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= csrf_meta_tags %>
    <title><%= content_for?(:title) ? content_for(:title) : "Microapp.io" %></title>

    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <%#= For all other devices %>
    <%#= Size should be 32 x 32 pixels %>
    <%= favicon_link_tag 'http://cdn.androidblip.com/icn/com_defiant_supermarioentertainment____90218.png', :rel => 'shortcut icon' %>
  </head>

  <body>

  <!-- menu -->
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container" >
        <a class="brand" href="/">microapp.io</a>
        <div class="nav-collapse">
          <% if content_for?(:menu_header) %>
            <%= content_for(:menu_header)  %>
          <% else %>
            <ul class="nav">
              <li> <a href="/apps">Explore apps</a></li>
              <li class="active"><a href="/apps/new">Create an app</a></li>
            </ul>
          <% end %>
          <% if user_signed_in? %>
            <%= link_to destroy_user_session_path, :method => :delete , :class=>"btn btn-small btn-primary pull-right"  do %>
              Sign out <b class=" icon-off"></b>
            <% end %>
          <% else %>
            <%= link_to "Sign in", new_user_session_path, :class=>"btn btn-small btn-primary pull-right"  %>
          <% end %>

          <ul class="nav pull-right">
            <li>
              <a href="#" ><%= current_or_guest_username %></a>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <%= yield %>

  <!-- Javascripts ================================================== -->
  <!-- Placed at the end of the document so the pages load faster -->
  <%= javascript_include_tag "application" %>
  <%= content_for(:script)  %>

  <script type="text/javascript">
    var _gaq = _gaq || [];
    _gaq.push(['_setAccount', 'UA-30505488-1']);
    _gaq.push(['_trackPageview']);

    (function () {
        var ga = document.createElement('script');
        ga.type = 'text/javascript';
        ga.async = true;
        ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
        var s = document.getElementsByTagName('script')[0];
        s.parentNode.insertBefore(ga, s);
    })();
  </script>
  </body>
</html>
